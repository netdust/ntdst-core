# ntdst-core — project STATE (Layer C)

## Parked findings (from the field-types gates, 2026-08-23) — not fixed in-branch
- Metabox CSS/JS (~630 lines inside `echo` strings in `admin/MetaboxGenerator.php`) → extract to `assets/css/metabox.css` + `assets/js/metabox-fields.js`, one enqueue, no `static` once-guards. Separate wave; contract change (enqueued asset vs inline).
- Double unslash on a metabox save: the metabox unslashes and `update_metadata()`/`wp_update_post()` unslash again → a literal backslash loses one level (pre-5.0 too). Own systematic-debugging cycle (`wp_update_post` expects slashed input; `validateData()` measures length). README "known" row in 5.0.0.
- `control_media` does `get_post_type()` + `wp_get_attachment_image_url()` + `get_the_title()` per media cell per row; `_prime_post_caches()` in `render_repeater_field()` would collapse it. Perf, pre-existing.
- `bin/guard.sh`'s REMOVED regex and `PackageBootIntegrityTest::removedSymbolProvider()` are one list in two homes → guard reads the provider (field-types T09 / compounding).
- Open ruling for Stefan: a retired type name at `register()` is a fatal (spec, plan threat row 7). Sentinel: 211 retired declarations on seven fleet sites, wp-cli dies too; alternative = catch in `NTDST_Data_Manager::register()` → error log + `WP_Error`, one model dropped. Kept the fatal pending Stefan's word.
- Browser look at daan's edit screen blocked (wp-admin login; throwaway admin creation denied by the permission classifier) → shakeout S2 once Stefan logs the superpowers Chrome profile into daan.ddev.site.
- REST meta writes run the vocabulary and skip `required`/`min`/`max`/`validate` (Cluster B sentinel I3) — README row in T09; a validator on the REST path is a later spec.
- Fifth ntdst-core consumer not in field-types FR-8: `stride/web/app/plugins/netdust-lti` (nested git repo) declares retired type names (`src/Shared/LTIDataService.php:179,206,284,433`; tests pin `'boolean'`). Rename before stride bumps to 5.0. (T08, 2026-08-23)
- stride `price_modifier` stays `signed_int` on the 3.x core it pins; rename to `int` in the 5.0 bump commit. (T08 ruling)
- daan's REAL branch needs at the 5.0 bump: tests require `api/FieldTypes.php` before `api/Data.php`; `new NTDST_Data_Model(type, schema, prefix, scopes)` (4th positional `public_fields` is gone). Proven on `chore/core-path-repo` @ ed242c9 (never merged).
- Shakeout manifest (field-types): S1 double-save idempotence — PASS 2026-08-23; S2 browser look at daan's edit screen (AF-1, needs a logged-in Chrome profile); S3 double-unslash — REPRODUCED 2026-08-23 (fix is its own cycle); S4 josworld full `composer gate` after `composer install` restores `ntdst-baseline/support/Cidr.php` (incl. `tests/Integration/CaseServiceEditorScreenTest.php`); S5 todai-client `composer gate` with DDEV port 44626 free. SC-6 is NOT met until S4/S5 are green.
- RELEASE GATE (core-trim): `feat/core-shape` must not reach stride before core-trim T11 — stride's `SecurityService.php:60`/`PerformanceService.php:39` still read `ntdst_service_security_config`; after the rename to `ntdst/service/{slug}/config` the override is inert and the all-false defaults re-open XML-RPC, version disclosure and login enumeration silently. stride pins 3.0.0 today. (sentinel, 2026-08-23)
- Fleet test breakage for the core-trim migration table: stride `tests/Unit/NtdstBootstrapTest.php:145` (`getServices`), daan `tests/Integration/BootstrapServiceVocabularyTest.php` (`hasService`/`getBootedServices`/`getServiceConfig`), daan `tests/Unit/DataScopesTest.php` (requires `api/Data.php` without `admin/MetaboxGenerator.php`).
- core-trim Cluster B (2026-08-23): fleet TEST breakage by FR-4/5/11 enumerated in `specs/core-trim/tasks.md` (T12 row) — daan ×9 files, josworld `DataLayerRoundTripTest:38` (josworld IS affected by FR-11), stride `RevealSensitiveFieldAuditTest:205`. No fleet PRODUCTION listener on `ntdst_log*`/`ntdst_model_*` outside daan's PressKitService (T12).
- daan T12 (core-trim) landed on chore/core-path-repo (da83e70, 6e6af1b); `tests/Integration/BootstrapServiceVocabularyTest.php` NOT adapted (three removed accessors, no mechanical mapping) — needs the destructive Integration suite run on a disposable DB. Shakeout item.
- stride `chore/core-trim` (T11, never merged): f48a8bf…2d5f16f (ten commits: Mailer move, reminders, config-filter bridge, tests, symlink-escape fix, cc/bcc, fail-closed mail guard, is_wp_error fix) — Mailer lives in netdust-mail; ntdst-auth depends on it; stride still pins ^3.0. Pre-existing bug found: `netdust-mail/src/MailService.php:266` dead `is_wp_error()` branch logs a failed send as "Email sent" — own cycle. Shakeout: one real send renders from netdust-mail's layout (no theme override exists).
- stride: the service-config reads bridge both filter names (trait `ServiceConfig`, dc722d4 on chore/core-trim); delete the fallback in the same commit that bumps ntdst-core to 5.0. AF-5 must be re-run after that bump.
- INV-9 zero-readers (T13): `ntdst_inline()` has no reader on the fleet (exempted as the unread half of a documented pair — core-shape decides); 28 ADVISORY method candidates with no fleet reader (incl. `Rest::put()`/`patch()`/`bucket()`, `Pages::url()`, `Theme::archive()`) — a later trim spec.
- stride ntdst-auth (pre-existing, found 2026-08-23 by the netdust-mail security pass — NOT core-trim's): rate limits trust `X-Forwarded-For`/`CF-Connecting-IP` without a trusted-proxy check (`TokenHelper::getClientIp` + two copies) → unbounded registration mail to any address, password brute force when `enable_password` is on; no per-email limit on `register()`; `magic_link_max_uses = 3` with the token in the URL path; `not_activated` is an account-existence oracle. Own spec.
- stride netdust-mail (notes for its own spec): `attach()` pushes `$path` not `$real`; the `ntdst_mail_attachment_bases` filter is gone — PDF generators must write under uploads (verify at shakeout); `layout.php` echoes `$content` raw while claiming `wp_kses_post`; `MailBuilder::attach()` extra attachments are never read; `netdust-mail.php:29` unconditional `require_once vendor/autoload.php`.
- core-trim COMPLETE 2026-08-23 (13/13; core 0f5d58c code / 1f8b219 docs; stride chore/core-trim ten commits; daan chore/core-path-repo three commits). Owed: a human reads README's 88-row migration record before v5.0.0 is tagged; shakeout: SC-6 two-request 0-notices on daan, daan Integration suite on a disposable DB, AF-5 after stride's pin bump, one real send from netdust-mail's layout.
- Stefan's rulings 2026-08-23: tag scheme is BARE `5.0.0` (move the remote tag at release); spec amendments approved; he reads README's 5.0.0 section himself before the tag; the retired-type-name fatal at `register()` stays.
- core-shape Cluster 3 blast radius (measured 2026-08-23): `ntdst_actions()` command-router call sites in shipped consumer code — stride 119, rossi 49, netdust 40, daan 28 (+ its theme's own `ntdstAPI` JS client), ludoluykx 28, josworld 20, todai-client 7. Each site migrates to `ntdst_rest()` routes in its own spec (D5: breakage accepted). The re-confirmation yield is after Cluster 3's gate.

## core-shape Cluster 3 — fleet notes (2026-08-23, from the FULL gate)
- stride `stride-core/Modules/Questionnaire/QuestionnaireHandler.php:32-37` registers two ANONYMOUS WRITES on the deleted dispatcher (`stride_submit_interest`, `stride_submit_waitlist`). On 5.0 `->public()` is refused on a write verb; the only spelling is `->post('/interest', $cb, ['permission' => fn(): bool => true, 'rate_limit' => 30])` (README FR-7 row added at the Cluster 3 fix wave). Without it an author reaches for raw `register_rest_route()` — the second door the cluster removed.
- stride production readers of `ntdst/api/rate_limit/{action}` now apply to nothing: `Trajectory/Admin/TrajectoryAdminController.php:108,115`, `Invoicing/Admin/QuoteAdminController.php:66`, `Edition/Admin/EditionAdminController.php:143` → `'rate_limit' => N` on the route (migration row added).
- Fleet call sites re-measured (GNU grep -F; see `.superpowers/sdd/plan/progress.md` "Cluster 3 gate — reports" for the command): stride 111 php / 30 js · rossi 67 / 34 · netdust 7 / 38 · daan 27 / 6 · ludoluykx 63 / 0 · josworld 7 / 14 · todai-client 11 / 0. The earlier table (119/49/40/28/28/20/7) was a ugrep artefact.
- Sentinel residual (not a defect): a CPT at default `capability_type => 'post'` maps `edit_others_posts` to the site-wide cap, so a `public => false` CPT that never remapped its caps is enumerable (published id+title) by any Editor through the picker route — same floor the old `ntdst_api_floor_cap()` computed; narrowing belongs to the CPT declaration.
- Lost with Actions.php, accepted by plan row #8: origin check for non-cookie auth schemes (Application Passwords/Basic/JWT) — a browser never attaches those by itself; `ntdst/api/public_actions` as a site-overridable register of anonymous reach (closing a third-party anonymous route now needs `rest_pre_dispatch`).
- T13 must resolve: README:81 documents an `ntdst_rest_pf_*` OPTIONS preflight bucket and `docs/findings-2026-08-20-from-todai-intake.md:24` records it fixed, but no such code exists (README:232 says OPTIONS is unmetered). Delete the claim or implement it.
- Shakeout (core-shape Cluster 4a, added 2026-08-23): template loader on a real child theme where stylesheet ≠ template dir; a real multisite `switch_to_blog()` loop proving the per-theme cache key; a legacy theme relying on `wp-includes/theme-compat` (now refused by `isInsideTheme()` — the caller gets WordPress's own path). TOCTOU between `realpath()` and `include` is not closable in PHP's include model — accepted.
- Shakeout (core-shape Cluster 4b, added 2026-08-23): on daan's proof branch — real `WP_Rewrite` ordering of `^card…` against page slugs; a Permalinks-save (external flush) followed by `/card/` still answering; canonical 301 on `/card` and on the file-like `/card/contact.vcf`; a placeholder route hit with `?ntdst_page=N&ntdst_p_x[]=1` and with the param absent (expect 404 after the Cluster 4b fix wave); daan `streamVCard()` body ends at its declared Content-Length (no theme page appended).
- core-shape T11 consumer note (2026-08-23): five fleet themes (daan, netdust, edushare, josworld, sc1-stackwp) carry `helpers/templates.php:28-30` promising "every key is also extracted as a loose variable" — false on 5.0 (`html()` → `load_template`, templates read `$args['key']`; WordPress's query vars are extracted first, so a loose `$name`/`$page`/`$s` reads the query var). At each bump: fix the docblock, drop the redundant `'args' =>` key in `daan_template_html()`-style wrappers.
- Next spec cycle candidate (from the Cluster 4b simplicity review, 2026-08-23): `NTDST_Response::reset()/template()/getTemplate()/error()/getStatus()` have zero fleet callers (grep over every `~/Sites/*/web/app/{mu-plugins,themes}`) but `ResponseTrimTest::testTheSurvivingSurfaceIsStillThere` pins them; `ntdst_response()` always `new`s, so `reset()`'s premise never occurs. Decide in a spec, not in a fix wave. Also: `Pages::notFound()` and `Response::notFound()` write the same three lines — converge on one `ntdst_send_404()` when a third shared file is acceptable.
- Shakeout (core-shape Cluster 5, added 2026-08-23): ludoluykx `ntdstheme/services/` has 17 `ntdst_theme()->single/archive/page(` call sites that fatal on 5.0 until renamed to `ntdst_pages()->…` (T12); an unconfigured theme on daan renders WordPress's own excerpt length after `after_setup_theme` (core no longer mounts `excerpt_length`/`excerpt_more` unless configured).
- Next doc cycle (from T13, 2026-08-23): six v3.0.0-era symbols banned by `bin/guard.sh` (`ntdst_api_action`, `ntdst_router`, `ntdst_route(`, `NTDST_Router`, `NTDST_Endpoints`, `ntdst_endpoints`) have no README migration row (no test requires one — `testEveryRemovedFiveOhSymbolHasAMigrationRow` filters on 5.0.0); `docs/plans/2026-08-16-download-dispatch-v2.3.md` still describes `GET /download` as live design (dated plan, left).
- daan port notes (Cluster 5 reviewer, 2026-08-23): `CardService.php:55,:63,:77` return `ntdst_response()->notFound()` from page callbacks — on 5.0 that is an object return (warning + 404); the port writes `return false;`. `DiscographyService.php:238` `'public_fields'` register() key is silently ignored on 5.0; `:349` `publicRows()` → `ntdst_data()->get('release')->where(...)`; `:378` `publicRow($id, $status)` → `getMeta($id, null, null, $status)`. `themes/daan/src/main.js:103` hand-rolled `window.ntdstAPI` 404s at runtime on `/get_nonce`/`/action`. `AccessGrantService.php:369` comment still names `ntdst_api_action`.
- Whole-branch review suggestions parked (2026-08-23, next cycle): S-1 locate() cache key omits the path registry (addPath-after-resolution not seen for an already-cached name); S-2 FieldTypes:663-665 re-inlines declaredType()/hardcodes image|file; S-3 taxonomies default public beside a private-by-default CPT; S-4 naming drift (five get(), three page(), three template()) degrades INV-9's method half; S-5 INV-9 is workstation-only (say so in the doc); S-6 Logger file name UTC vs stamp site-local (a 01:30 line lands in yesterday's file east of UTC); S-7 downloadHeaders() emits nosniff from a builder (sendDownloadHeaders() wrapper); S-8 Pages::url() str_replace prefix collision (:slug vs :slug_id); S-9 single()/archive()/page() can hand null to a callable; S-10 conditional page routes flip-flop the flush hash (docblock sentence); S-11 a declared field named `title`/`post_status` re-enters the save allow-list and routes to wp_posts columns (refuse WP_COLUMNS keys in the model ctor); S-12 three files, three rules for load-time hook mounting.
- Repeater shakeout probe (I-2): add 3 rows, save, delete the middle row, add a row, save — all three survive with the new one appended.
- Shakeout CLOSED 2026-08-23: daan edit-screen browser look PASS; repeater add-after-delete probe PASS in a real browser (new index = max+1, no reuse; nothing saved). ludoluykx renames BLOCKED by design — pinned core 2.0.0 has no ntdst_pages(); do the renames as part of its 5.0 bump commit.
- josworld (2026-08-23): pin is ^5.0 locked to 5.0.0 on chore/ntdst-core-4 — the "deliberately not adapted" note was stale. Its unit tier fatals on `tests/Unit/BaselineInputHandlingTest.php:13` (requires ntdst-baseline's dropped `support/Cidr.php`) — FIXED at `2773095` on chore/ntdst-core-4 — require re-pointed to core's Cidr.php+ClientIp.php; unit tier OK (113 tests, one tier).
- netdust-mail (2026-08-23): `templates/emails/layout.php` never registers on `ntdst_email_layout_paths` — dead on every real send; core's inline fallback renders instead. FluentSMTP force-overrides the From header on stride. Fix in the next netdust-mail cycle.
